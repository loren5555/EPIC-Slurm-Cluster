---
# Copy this structure with `ansible-vault create vars/secrets.yml`.
# Use a generated password without `#` or newline characters because the value
# is written to slurmdbd.conf, where `#` begins a comment.
slurmdbd_storage_password: "replace-with-a-generated-password"
