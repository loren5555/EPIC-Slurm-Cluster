---
# Maintain filesystem quota policy after identity convergence. Only hosts that
# declare disk_quotas run the role, so future infrastructure hosts stay idle.
- name: Maintain local filesystem quota policy
  hosts: controllers:compute_nodes
  become: true
  vars_files:
    - ../vars/users.yml
  roles:
    - role: disk_quota
      when: disk_quotas is defined
