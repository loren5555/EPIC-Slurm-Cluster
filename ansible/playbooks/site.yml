---
# Full convergence entry point. Identities must exist before SSH authorization.
- import_playbook: users.yml  # Identity related settings.
- import_playbook: ssh_access.yml  # Cluster SSH keys and per-host access.
- import_playbook: slurm.yml  # Scheduler configuration and host partitions.
- import_playbook: slurmdbd.yml  # Local accounting database and daemon.

# Future subsystems remain separate so their changes cannot accidentally alter
# the already stable identity and SSH roles.
# - ood.yml
