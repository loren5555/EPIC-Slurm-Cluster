# GPU Workflow Example

This is a Project Manager template. In Open OnDemand, select **Create a new
project**, choose **Example**, then create your own private copy.
The system-owned source is never edited by users.
