---
- name: Restart GPU exporters
  ansible.builtin.systemd_service:
    name: "{{ item }}"
    daemon_reload: true
    enabled: true
    state: restarted
  loop:
    - nvidia-dcgm-exporter.service
    - nvitop-exporter.service
  when: not ansible_check_mode
