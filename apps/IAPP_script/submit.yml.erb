<% require 'json'; require 'shellwords' %>
---
batch_connect:
  template: basic

script:
  queue_name: "<%= resource_partition %>"
  native:
    - "--cpus-per-task=<%= cpus_per_task.to_i %>"
    - "--mem=<%= memory.to_i %>G"
    - "--time=<%= bc_num_hours.to_i %>:00:00"
    - "--array=<%= array %>"
<% if gpus_per_task.to_i > 0 %>
    - "--gres=gpu:<%= gpus_per_task.to_i %>"
<% end %>
<% Shellwords.split(extra_sbatch.to_s).each do |argument| %>
    - <%= argument.to_json %>
<% end %>
