#!/usr/bin/env bash

set -euo pipefail

# Export the module function if it exists
[[ $(type -t module) == "function" ]] && export -f module

# code-server is installed on each compute host and resolved from PATH.

# Find available port to run server on
export port=$(find_port ${host})

# Export compute node the script is running on
export host="${host}"

# Generate SHA1 encrypted password (requires OpenSSL installed)
export password="$(create_passwd 16)"

export code_server_version=$(code-server -v | head -n1 | cut -d' ' -f1)
